# Azure AD Configuration
# You'll need to replace these with your actual Azure AD tenant and application values

# Your Azure AD tenant ID (found in Azure Portal > Azure Active Directory > Overview)
VITE_AZURE_TENANT_ID=your-tenant-id-here

# Your app registration client ID (found in Azure Portal > App registrations > your app > Overview)
VITE_AZURE_CLIENT_ID=your-client-id-here

# The redirect URI configured in your app registration (should match your app URL)
VITE_AZURE_REDIRECT_URI=http://localhost:5173

# Optional: Authority URL (defaults to common if not specified)
VITE_AZURE_AUTHORITY=https://login.microsoftonline.com/your-tenant-id-here